model Post {
  id        String    @id @default(uuid())
  title     String
  content   String
  likes     Int       @default(0)
  authorId  String
  comments  Json[]    // or separate Comment model for better relations
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt

  @@map("posts")
}
